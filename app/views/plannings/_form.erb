<p><%= label_tag(:name) %>
<%= text_field(:planning, :name) %></p>

<p><%= label_tag(:project_id) %>
<%= select("planning", "project_id", Project.find(:all, :conditions=>["name IS NOT NULL"]).collect {|p| [ p.name, p.id ] }) %></p>